import React from "react";
import PropTypes from "prop-types";

class ArtifactThumbnail extends React.Component {
  constructor(props) {
    super(props);

    this.state = {

    };
  }

  render() {
    return '';
  }
}

ArtifactThumbnail.propTypes = {
  blob_url: PropTypes.number.isRequired
};

export default ArtifactThumbnail;