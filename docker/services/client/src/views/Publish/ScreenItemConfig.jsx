import { withStyles } from '@material-ui/core';
import React from 'react';

class ScreenItemConfig extends React.Component {
  constructor(props) {
    super(props);

    this.props = props;
    this.state = {

    }
  }

  render() {
    const { classes } = this.props;

    return '';
  }
}

export default withStyles(ScreenItemConfig);