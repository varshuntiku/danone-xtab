const linearProgressStyle = (theme) => ({
    root: {
        height: theme.spacing(1)
    },
    barColorPrimary: {
        backgroundColor: theme.palette.primary.contrastText
    }
});

export default linearProgressStyle;
