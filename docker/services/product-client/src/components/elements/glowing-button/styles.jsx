const glowingButtonStyles = (theme) => ({
    loginButtonEmail: {
        width: '100% !important',
        cursor: 'pointer',
        borderRadius: 0
    },
    loginButtonEmailRoot: {
        borderRadius: 0,
        fontSize: theme.spacing(2.5),
        fontWeight: 900
    }
});

export default glowingButtonStyles;
