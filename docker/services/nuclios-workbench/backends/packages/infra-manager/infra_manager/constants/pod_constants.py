POD_SPEC = {"container_ports": [{"containerPort": 80}]}

POD_ERRORS = {
    "pod_spec_empty": "Pod Specification is required and must a valid V1PodSpec Object",
    "pod_create_failed": "Pod Creation Failed Check the error log for more info",
}
